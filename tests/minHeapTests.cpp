#include "includes/doctest.h"
#include <unistd.h>
#include "../includes/MinHeap.hpp"

TEST_CASE("sleep a second") {
    MinHeap<int> h;
    CHECK(0 == 0);
}

TEST_CASE("sleepy Rosi") {
    MinHeap<int> h;
    CHECK(0 == 0);
}

// int main(){
//     return 0;
// }